// Vitest global setup. Loads jest-dom matchers for component tests.
//
// jest-dom only takes effect in tests that opt into the jsdom environment via
// `// @vitest-environment jsdom` at the top of the file. Node-environment
// tests are unaffected.
import "@testing-library/jest-dom/vitest";
